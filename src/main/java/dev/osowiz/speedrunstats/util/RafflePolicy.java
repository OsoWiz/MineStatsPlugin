package dev.osowiz.speedrunstats.util;

public enum RafflePolicy {
    /**
     * Todo possible raffle enum policies.
     * These could be used to determine the specifics of how teams are selected.
     */
    RANDOM,
    MINIMIZE_RANK_DISPARITY,

}
